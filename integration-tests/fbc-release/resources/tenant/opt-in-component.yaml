---
apiVersion: appstudio.redhat.com/v1alpha1
kind: Component
metadata:
  annotations:
    git-provider: github
    build.appstudio.openshift.io/request: configure-pac
    image.redhat.com/generate: '{"visibility": "public"}'
    build.appstudio.openshift.io/pipeline: '{"name": "fbc-builder", "bundle": "latest"}'
  name: ${opt_in_component_name}
  labels:
    originating-tool: "${originating_tool}"
spec:
  application: optin-${application_name}
  componentName: ${opt_in_component_name}
  secret: pipelines-as-code-secret-${component_name}
  source:
    git:
      dockerfileUrl: catalog.Dockerfile
      revision: ${opt_in_component_branch}
      context: "v4.13"          # Same context for opt-in batching
      url: "${opt_in_component_git_url}"
