---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: test-rh-sign-cosign-image-multiple-components
spec:
  description: Test signing a single image with cosign 
  workspaces:
    - name: tests-workspace
  tasks:
    - name: setup
      workspaces:
        - name: data
          workspace: tests-workspace
      taskSpec:
        workspaces:
          - name: data
        steps:
          - name: setup-values
            image: quay.io/konflux-ci/release-service-utils:38c3bfd7479c86b832cba5b61f9bbde40c469393
            script: |
              #!/usr/bin/env sh
              set -eux
              _TEST_MANIFEST_LIST_DIGEST="sha256:1111"
              _TEST_MANIFEST_LIST_MDIGEST1="sha256:1111-1"
              _TEST_MANIFEST_LIST_MDIGEST2="sha256:1111-2"
              _TEST_MANIFEST_LIST_MDIGEST3="sha256:1111-3"
              _TEST_MANIFEST_LIST_REFERENCE="quay.io/redhat-pending/test-product----test-image0@${_TEST_MANIFEST_LIST_DIGEST}"
              _TEST_MANIFEST_LIST_REPO="quay.io/redhat-pending/test-product----test-image0"

              _TEST_MANIFEST_LIST_OCI_DIGEST="sha256:0000"
              _TEST_MANIFEST_LIST_OCI_MDIGEST1="sha256:0000-1"
              _TEST_MANIFEST_LIST_OCI_MDIGEST2="sha256:0000-2"
              _TEST_MANIFEST_LIST_OCI_MDIGEST3="sha256:0000-3"
              _TEST_MANIFEST_LIST_OCI_REFERENCE="quay.io/redhat-pending/test-product----test-image0@${_TEST_MANIFEST_LIST_OCI_DIGEST}"
              _TEST_MANIFEST_LIST_OCI_REPO="quay.io/redhat-pending/test-product----test-image0"

              cat > $(workspaces.data.path)/snapshot_spec.json << EOF
              {
                "application": "myapp",
                "components": [
                  {
                    "name": "comp0",
                    "containerImage": "$_TEST_MANIFEST_LIST_REFERENCE",
                    "repository": "$_TEST_MANIFEST_LIST_REPO"
                  },
                  {
                    "name": "comp0",
                    "containerImage": "$_TEST_MANIFEST_LIST_OCI_REFERENCE",
                    "repository": "$_TEST_MANIFEST_LIST_OCI_REPO"
                  }
                ]
              }
              EOF

              cat > $(workspaces.data.path)/data.json << EOF
              {
                "sign": {
                  "request": "hacbs-signing-pipeline",
                  "configMapName": "signing-config-map"
                }
              }
              EOF
    - name: run-task
      taskRef:
        name: rh-sign-cosign-image
      params:
        - name: snapshotPath
          value: snapshot_spec.json
        - name: dataPath
          value: data.json
        - name: secretName
          value: 'test-rh-sign-cosign-image-secret'
      workspaces:
        - name: data
          workspace: tests-workspace
      runAfter:
        - setup
    - name: check-result
      workspaces:
        - name: data
          workspace: tests-workspace
      taskSpec:
        steps:
          - name: check-result
            image: quay.io/konflux-ci/release-service-utils:38c3bfd7479c86b832cba5b61f9bbde40c469393
            script: |
              #!/usr/bin/env sh
              set -eux
              echo "check results"
              _TEST_PUB_REPO="registry.stage.redhat.io/test-product/test-image0"
              _TEST_REPO="quay.io/redhat-pending/test-product----test-image0"

              EXPECTED=$(cat <<EOF
              inspect --raw docker://${_TEST_REPO}@sha256:1111
              inspect --raw docker://${_TEST_REPO}@sha256:0000
              EOF
              )
              CALLS=$(cat "$(workspaces.data.path)/mock_skopeo_calls")
              [ "$CALLS" = "$EXPECTED" ] || exit 1
              CALLS=$(cat "$(workspaces.data.path)/mock_cosign_calls")
              PRE_ARGS="AWS_REGION=us-test-1"
              PRE_ARGS="$PRE_ARGS AWS_ACCESS_KEY_ID=test-access-key"
              PRE_ARGS="$PRE_ARGS AWS_SECRET_ACCESS_KEY=test-secret-access-key"

              COMMON_ARGS="-t 3m0s sign --key aws://arn:mykey"
              COSIGN_COMMON="$COMMON_ARGS --sign-container-identity"
              EXPECTED=$(cat <<EOF
              $COSIGN_COMMON ${_TEST_PUB_REPO}@sha256:1111-1 ${_TEST_REPO}@sha256:1111-1
              $COSIGN_COMMON ${_TEST_PUB_REPO}@sha256:1111-2 ${_TEST_REPO}@sha256:1111-2
              $COSIGN_COMMON ${_TEST_PUB_REPO}@sha256:1111-3 ${_TEST_REPO}@sha256:1111-3
              $COSIGN_COMMON ${_TEST_PUB_REPO}@sha256:1111 ${_TEST_REPO}@sha256:1111
              $COSIGN_COMMON ${_TEST_PUB_REPO}@sha256:0000-1 ${_TEST_REPO}@sha256:0000-1
              $COSIGN_COMMON ${_TEST_PUB_REPO}@sha256:0000-2 ${_TEST_REPO}@sha256:0000-2
              $COSIGN_COMMON ${_TEST_PUB_REPO}@sha256:0000-3 ${_TEST_REPO}@sha256:0000-3
              $COSIGN_COMMON ${_TEST_PUB_REPO}@sha256:0000 ${_TEST_REPO}@sha256:0000
              EOF
              )
              test "$CALLS" = "$EXPECTED" 
      runAfter:
        - run-task
