---
# Declarative mocks for Tekton tests of this task (Python entrypoint). Rendered
# by .github/scripts/render_python_task_mocks_from_yaml.py when
# test_tekton_tasks.sh runs.
#
# No HTTP mock services are needed: extract uses stdlib zipfile on a real
# wheel created in setup. The remaining bash upload step is mocked via
# tests/mocks.sh (injected by pre-apply-task-hook.sh).
version: 1
