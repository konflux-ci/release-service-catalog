#!/usr/bin/env bash
# Mock openssl for GitHub App JWT signing in Tekton tests.
set -euo pipefail

if [[ "${1:-}" == "dgst" && "${2:-}" == "-sha256" && "${3:-}" == "-sign" ]]; then
  printf '%s' 'mock-jwt-signature'
  exit 0
fi

exec command openssl "$@"
