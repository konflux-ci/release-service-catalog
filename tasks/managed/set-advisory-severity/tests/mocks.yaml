---
# Declarative mocks for Tekton tests of this task (Python entrypoint). Rendered
# by .github/scripts/render_python_task_mocks_from_yaml.py when
# test_tekton_tasks.sh runs.
#
# No HTTP mock services are needed. InternalRequest creation and status reads
# go through the Kubernetes Python client, which is mocked in
# tests/python_mocks/sitecustomize.py.
version: 1
