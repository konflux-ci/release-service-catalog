#!/usr/bin/env bash
set -euo pipefail

SUBCMD="${1:-}"

case "${SUBCMD}" in
  resolve)
    echo "sha256:abc123def456"
    ;;
  cp)
    exit 0
    ;;
  discover)
    echo '{"referrers":[]}'
    ;;
  *)
    exit 0
    ;;
esac
