---
# Declarative mocks for Tekton tests of this task (Python entrypoint). Rendered
# by .github/scripts/render_python_task_mocks_from_yaml.py when
# test_tekton_tasks.sh runs.
#
# No HTTP mock services are needed: all external calls go through CLI tools
# (oras, rpm, kubectl, internal-request, select-oci-auth) which are mocked via
# executable scripts in the tests/mocks/ directory.
version: 1
