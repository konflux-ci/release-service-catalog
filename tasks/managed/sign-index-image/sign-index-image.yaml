---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: sign-index-image
  annotations:
    tekton.dev/pipelines.minVersion: "0.12.1"
    tekton.dev/tags: release
spec:
  description: |-
    Creates an InternalRequest to sign an index image

    ## Signing data parameters

     The signing configuration should be set as `data.sign` in the _releasePlanAdmission_. Previously it used to be
     set also in the `data.fbc`. The data should be set in the _ReleasePlanAdmission_ as follows:

    ```
    data:
        sign:
            request: <signing pipeline name>
            pipelineImage: <image pullspec>
            configMapName: <configmap name>
    ```
  params:
    - name: dataPath
      description: Path to the JSON string of the merged data to use in the data workspace
      type: string
    - name: releasePlanAdmissionPath
      description: Path to the JSON string of the releasePlanAdmission in the data workspace
      type: string
    - name: requester
      type: string
      description: Name of the user that requested the signing, for auditing purposes
    - name: requestTimeout
      type: string
      default: "1800"
      description: InternalRequest timeout
    - name: pipelineRunUid
      type: string
      description: The uid of the current pipelineRun. Used as a label value when creating internal requests
    - name: fbcResultsPath
      type: string
      description: Path to the JSON file in the data workspace containing fbc results
    - name: concurrentLimit
      type: string
      description: The maximum number of concurrent signing requests
      default: 16
    - name: ociStorage
      description: The OCI repository where the Trusted Artifacts are stored
      type: string
      default: "empty"
    - name: ociArtifactExpiresAfter
      description: Expiration date for the trusted artifacts created in the
        OCI repository. An empty string means the artifacts do not expire
      type: string
      default: "1d"
    - name: trustedArtifactsDebug
      description: Flag to enable debug logging in trusted artifacts. Set to a non-empty string to enable
      type: string
      default: ""
    - name: orasOptions
      description: oras options to pass to Trusted Artifacts calls
      type: string
      default: ""
    - name: sourceDataArtifact
      type: string
      description: Location of trusted artifacts to be used to populate data directory
      default: ""
    - name: dataDir
      description: The location where data will be stored
      type: string
      default: /var/workdir/release
    - name: taskGitUrl
      type: string
      description: The url to the git repo where the release-service-catalog tasks and stepactions to be used are stored
    - name: taskGitRevision
      type: string
      description: The revision in the taskGitUrl repo to be used
    - name: pyxisServer
      type: string
      description: The server type to use. Options are 'production','production-internal,'stage-internal' and 'stage'
      default: production
    - name: pyxisSecret
      type: string
      description: |
        The kubernetes secret to use to authenticate to Pyxis. It needs to contain two keys: key and cert
    - name: batchLimit
      type: string
      description: |
        size of batch attributes to send to internal-request. As internal request arguments are need to be
        strings, size here represent maximal string length of `references` and `manifest_digests` sent to
        internal request
      default: 4096
    - name: caTrustConfigMapName
      type: string
      description: The name of the ConfigMap to read CA bundle data from
      default: trusted-ca
    - name: caTrustConfigMapKey
      type: string
      description: The name of the key in the ConfigMap that contains the CA bundle data
      default: ca-bundle.crt
    - name: failOnSignatureLookupError
      type: string
      default: "true"
      description: |
        Fail the task when any Pyxis find_signatures lookup fails; when set to "false", log a warning
        and submit every planned image row for signing without skipping already-signed references
    - name: signatureLookupMaxAttempts
      type: string
      default: "3"
      description: |
        Maximum number of attempts per Pyxis find_signatures lookup including the first; waits
        (attempt-1)*2 seconds before each retry after the first; use "1" to disable retries
  results:
    - name: sourceDataArtifact
      type: string
      description: Produced trusted data artifact
  volumes:
    - name: workdir
      emptyDir: {}
    - name: pyxis-secret-vol
      secret:
        secretName: $(params.pyxisSecret)
        defaultMode: 0444
    - name: trusted-ca
      configMap:
        name: $(params.caTrustConfigMapName)
        items:
          - key: $(params.caTrustConfigMapKey)
            path: ca-bundle.crt
        optional: true

  stepTemplate:
    securityContext:
      runAsUser: 1001
    volumeMounts:
      - mountPath: /var/workdir
        name: workdir
      - name: trusted-ca
        mountPath: /mnt/trusted-ca
        readOnly: true
    env:
      - name: IMAGE_EXPIRES_AFTER
        value: $(params.ociArtifactExpiresAfter)
      - name: "ORAS_OPTIONS"
        value: "$(params.orasOptions)"
      - name: "DEBUG"
        value: "$(params.trustedArtifactsDebug)"
  steps:
    - name: use-trusted-artifact
      computeResources:
        limits:
          memory: 64Mi
        requests:
          memory: 64Mi
          cpu: 30m
      ref:
        resolver: "git"
        params:
          - name: url
            value: $(params.taskGitUrl)
          - name: revision
            value: $(params.taskGitRevision)
          - name: pathInRepo
            value: stepactions/use-trusted-artifact/use-trusted-artifact.yaml
      params:
        - name: workDir
          value: $(params.dataDir)
        - name: sourceDataArtifact
          value: $(params.sourceDataArtifact)
    - name: sign-index-image
      image: quay.io/lgallovi-test/release-service-utils@sha256:4cc5176e207653d7792714a2766c3fc4fff5efd60a6b095f4bbdc8f2772072ef
      computeResources:
        limits:
          memory: 1280Mi
        requests:
          memory: 1280Mi
          cpu: 300m
      volumeMounts:
        - name: pyxis-secret-vol
          mountPath: "/etc/secrets"
      env:
        - name: DATA_FILE
          value: "$(params.dataDir)/$(params.dataPath)"
        - name: FBC_RESULTS_FILE
          value: "$(params.dataDir)/$(params.fbcResultsPath)"
        - name: RPA_FILE
          value: "$(params.dataDir)/$(params.releasePlanAdmissionPath)"
        - name: REQUESTER
          value: "$(params.requester)"
        - name: REQUEST_TIMEOUT
          value: "$(params.requestTimeout)"
        - name: PIPELINE_RUN_UID
          value: "$(params.pipelineRunUid)"
        - name: CONCURRENT_LIMIT
          value: "$(params.concurrentLimit)"
        - name: PYXIS_SERVER
          value: "$(params.pyxisServer)"
        - name: BATCH_LIMIT
          value: "$(params.batchLimit)"
        - name: FAIL_ON_SIGNATURE_LOOKUP_ERROR
          value: "$(params.failOnSignatureLookupError)"
        - name: SIGNATURE_LOOKUP_MAX_ATTEMPTS
          value: "$(params.signatureLookupMaxAttempts)"
        - name: TASK_RUN_UID
          value: "$(context.taskRun.uid)"
        - name: TASK_GIT_URL
          value: "$(params.taskGitUrl)"
        - name: TASK_GIT_REVISION
          value: "$(params.taskGitRevision)"
        - name: PYXIS_CERT_PATH
          value: /etc/secrets/cert
        - name: PYXIS_KEY_PATH
          value: /etc/secrets/key
      command: ["python3", "-m", "sign_index_image"]
    - name: create-trusted-artifact
      computeResources:
        limits:
          memory: 128Mi
        requests:
          memory: 128Mi
          cpu: 250m
      ref:
        resolver: "git"
        params:
          - name: url
            value: $(params.taskGitUrl)
          - name: revision
            value: $(params.taskGitRevision)
          - name: pathInRepo
            value: stepactions/create-trusted-artifact/create-trusted-artifact.yaml
      params:
        - name: ociStorage
          value: $(params.ociStorage)
        - name: workDir
          value: $(params.dataDir)
        - name: sourceDataArtifact
          value: $(results.sourceDataArtifact.path)
