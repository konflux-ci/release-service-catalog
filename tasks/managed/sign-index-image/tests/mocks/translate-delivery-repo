#!/usr/bin/env bash
echo "Mock translate-delivery-repo called with: $*" >&2
# Return a redhat.io translation for any input reference
ref="$1"
# Strip the quay.io/redhat/redhat---- prefix and convert to registry.redhat.io
simple="${ref#quay.io/}"
cat <<EOF
[{"repo": "quay.io", "url": "${ref}"}, {"repo": "redhat.io", "url": "${ref}"}]
EOF
