---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: push-rpms-to-pulp
spec:
  description: |
    A task to push rpm packages from an OCI artifact to a Pulp repository.
  params:
    - name: SNAPSHOT_PATH
      type: string
      description: Path to the snapshot spec file containing image information
      default: ""
    - name: signedRpmsOciArtifact
      type: string
      description: |
        OCI artifact reference containing signed RPMs (e.g., oci:registry/repo@sha256:...).
        When provided, RPMs are extracted from this artifact instead of from the snapshot.
        Either SNAPSHOT_PATH or signedRpmsOciArtifact must be provided
      default: ""
    - name: PULP_DOMAIN
      type: string
      description: The domain to use for Pulp operations
    - name: PULP_SECRET_NAME
      type: string
      description: >-
        The name of the secret containing the Pulp cli.toml file. It must have the cli.toml key
    - name: DEFAULT_EXCLUDES
      type: string
      description: comma-delimited list of file patterns to exclude from the upload
      default: "-debuginfo-, -debugsource-"
    - name: DEFAULT_ARCHITECTURES
      type: string
      description: >-
        Comma-delimited list of arch repos. Noarch RPMs are pushed to each of these repos.
        When no arch-specific RPMs are found (e.g. only src+noarch), this list is also used
        so the upload loop runs
      default: "x86_64,aarch64,s390x,ppc64le"
    - name: resultsDirPath
      description: Path to the results directory in the data workspace
      type: string
    - name: artifactsJsonDirPath
      description: Path to the directory where artifacts.json will be written (relative to dataDir)
      type: string
      default: ""
    - name: ociStorage
      description: The OCI repository where the Trusted Artifacts are stored
      type: string
      default: "empty"
    - name: ociArtifactExpiresAfter
      description: Expiration date for the trusted artifacts created in the
        OCI repository. An empty string means the artifacts do not expire
      type: string
      default: "1d"
    - name: trustedArtifactsDebug
      description: Flag to enable debug logging in trusted artifacts. Set to a non-empty string to enable
      type: string
      default: ""
    - name: orasOptions
      description: oras options to pass to Trusted Artifacts calls
      type: string
      default: ""
    - name: sourceDataArtifact
      type: string
      description: Location of trusted artifacts to be used to populate data directory
      default: ""
    - name: dataDir
      description: The location where data will be stored
      type: string
      default: /var/workdir/release
    - name: taskGitUrl
      type: string
      description: The url to the git repo where the release-service-catalog tasks and stepactions to be used are stored
    - name: taskGitRevision
      type: string
      description: The revision in the taskGitUrl repo to be used
    - name: caTrustConfigMapName
      type: string
      description: The name of the ConfigMap to read CA bundle data from
      default: trusted-ca
    - name: caTrustConfigMapKey
      type: string
      description: The name of the key in the ConfigMap that contains the CA bundle data
      default: ca-bundle.crt
    - name: pulpUploadChunkSize
      type: string
      description: The chunk size to use when uploading RPMs to Pulp
      default: "100MB"
    - name: pulpTaskTimeout
      type: string
      description: >-
        Maximum time in seconds to wait for Pulp async tasks to complete (default 7200 = 2 hours)
      default: "7200"
  results:
    - description: Produced trusted data artifact
      name: sourceDataArtifact
      type: string
  volumes:
    - name: workdir
      emptyDir: {}
    - name: pulp-secret-name-vol
      secret:
        secretName: $(params.PULP_SECRET_NAME)
        defaultMode: 0444
    - name: trusted-ca
      configMap:
        name: $(params.caTrustConfigMapName)
        items:
          - key: $(params.caTrustConfigMapKey)
            path: ca-bundle.crt
        optional: true

  stepTemplate:
    env:
      # Ensure Tekton creds-init can copy registry creds to a writable $HOME (avoid /tekton/home permission warnings).
      - name: HOME
        value: /tmp
      - name: SNAPSHOT_PATH
        value: $(params.dataDir)/$(params.SNAPSHOT_PATH)
      - name: DEFAULT_EXCLUDES
        value: $(params.DEFAULT_EXCLUDES)
      - name: DEFAULT_ARCHITECTURES
        value: $(params.DEFAULT_ARCHITECTURES)
      - name: DATA_DIR
        value: $(params.dataDir)
      - name: PULP_DOMAIN
        value: $(params.PULP_DOMAIN)
      - name: PULP_SECRET_NAME
        value: $(params.PULP_SECRET_NAME)
      - name: RESULTS_DIR_PATH
        value: $(params.resultsDirPath)
      - name: ARTIFACTS_JSON_DIR_PATH
        value: $(params.artifactsJsonDirPath)
      - name: ociStorage
        value: $(params.ociStorage)
      - name: ociArtifactExpiresAfter
        value: $(params.ociArtifactExpiresAfter)
      - name: trustedArtifactsDebug
        value: $(params.trustedArtifactsDebug)
      - name: IMAGES_TXT
        value: $(params.dataDir)/images.txt
      - name: FILES_DIR
        value: /var/workdir/rpm-extract
      - name: SIGNED_RPMS_OCI_ARTIFACT
        value: $(params.signedRpmsOciArtifact)
      - name: IMAGE_EXPIRES_AFTER
        value: $(params.ociArtifactExpiresAfter)
      - name: ORAS_OPTIONS
        value: $(params.orasOptions)
      - name: DEBUG
        value: $(params.trustedArtifactsDebug)
      - name: PULP_UPLOAD_CHUNK_SIZE
        value: $(params.pulpUploadChunkSize)
      - name: PULP_TASK_TIMEOUT
        value: $(params.pulpTaskTimeout)
    volumeMounts:
      - name: workdir
        mountPath: "/var/workdir"
      - name: trusted-ca
        mountPath: /mnt/trusted-ca
        readOnly: true
    workingDir: "/var/workdir"
    securityContext:
      runAsUser: 1001
  steps:
    - name: use-trusted-artifact
      computeResources:
        limits:
          memory: 64Mi
        requests:
          memory: 64Mi
          cpu: 30m
      ref:
        resolver: "git"
        params:
          - name: url
            value: $(params.taskGitUrl)
          - name: revision
            value: $(params.taskGitRevision)
          - name: pathInRepo
            value: stepactions/use-trusted-artifact/use-trusted-artifact.yaml
      params:
        - name: workDir
          value: $(params.dataDir)
        - name: sourceDataArtifact
          value: $(params.sourceDataArtifact)
    - name: push-rpms-to-pulp
      image: quay.io/lgallovi-test/release-service-utils@sha256:10c63bb764f596ff6991ea27b792dd6c7c27b48009e512ca26939bcbcfb0f70b
      computeResources:
        limits:
          memory: 4Gi
        requests:
          memory: 4Gi
          cpu: 1
      volumeMounts:
        - name: pulp-secret-name-vol
          mountPath: "/etc/secrets"
      command:
        - python3
      args:
        - -u
        - -m
        - release_service_utils.tasks.managed.push_rpms_to_pulp
    - name: create-trusted-artifact
      computeResources:
        limits:
          memory: 128Mi
        requests:
          memory: 128Mi
          cpu: 250m
      ref:
        resolver: "git"
        params:
          - name: url
            value: "$(params.taskGitUrl)"
          - name: revision
            value: "$(params.taskGitRevision)"
          - name: pathInRepo
            value: stepactions/create-trusted-artifact/create-trusted-artifact.yaml
      params:
        - name: ociStorage
          value: $(params.ociStorage)
        - name: workDir
          value: $(params.dataDir)
        - name: sourceDataArtifact
          value: $(results.sourceDataArtifact.path)
