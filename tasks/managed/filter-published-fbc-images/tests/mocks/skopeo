#!/usr/bin/env bash
# Mock skopeo inspect --raw: return a single-arch manifest with an OCP base tag.
set -euo pipefail

cat <<'EOF'
{
  "mediaType": "application/vnd.oci.image.manifest.v1+json",
  "annotations": {
    "org.opencontainers.image.base.name": "registry.access.redhat.com/ubi9/ubi:4.15"
  }
}
EOF
