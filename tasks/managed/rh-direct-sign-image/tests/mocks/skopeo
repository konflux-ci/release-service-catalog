#!/usr/bin/env bash
echo "Mock skopeo called with: $*" >&2
# Return a single-manifest so get_all_image_digests adds no nested digests
echo '{"mediaType": "application/vnd.docker.distribution.manifest.v2+json", "schemaVersion": 2}'
