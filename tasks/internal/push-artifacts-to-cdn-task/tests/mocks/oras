#!/usr/bin/env bash
# Mock oras: handle login (no-op) and push (emit a digest line).
if [[ "${1}" == "login" ]]; then
  exit 0
elif [[ "${1}" == "push" ]]; then
  echo "Digest: sha256:$(echo "${*}" | sha256sum | awk '{print $1}')"
elif [[ "${1}" == "pull" ]]; then
  exit 0
fi
