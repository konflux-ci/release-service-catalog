#!/usr/bin/env bash

set -eu

# The test only exercises the task wiring; keep external OCI operations as
# successful no-ops while returning the digest format expected by oras_push.
if [[ "${1:-}" == "push" ]]; then
  echo "Digest: sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
fi
